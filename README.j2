# Development Environment

## Selected Operating System: {% if osVariant == 'windows' %}Windows{% elif osVariant == 'macos' %}macOS{% else %}Windows ARM{% endif %}

Open Terminal ( CTRL + ` )

[Access URL](https://__WEB__HOST__)

## Environment Details

{% if osVariant == 'windows' %}
### Windows Environment
- Standard Windows development environment
- Accessible via the web interface
- Windows Username: {{winUser}}
- Windows Password: {{winPass}}
- Tailscale integration available

{% elif osVariant == 'macos' %}
### macOS Environment
- macOS running in a Docker container
- Based on [dockur/macos](https://github.com/dockur/macos)
- Accessible via the web interface
- Provides native macOS development environment

{% else %}
### Windows ARM Environment
- Windows ARM running in a Docker container
- Based on [dockur/windows-arm](https://github.com/dockur/windows-arm)
- Accessible via the web interface
- Windows Username: {{winUser}}
- Windows Password: {{winPass}}
- Tailscale integration available
- Optimized for ARM architecture
{% endif %}

## Getting Started

1. Wait for the system to complete initialization
2. Access the environment through the web interface at the URL above
3. {% if osVariant in ['windows', 'windows-arm'] %}Use the provided Windows credentials to log in{% endif %}

## Features

- Full virtualization support
- Docker integration
- Web-based access
- Resource-optimized configuration
{% if osVariant in ['windows', 'windows-arm'] %}- Tailscale networking support{% endif %}

## System Requirements

- Minimum 4GB RAM available
- KVM virtualization support
- Docker with privileged container support

## Troubleshooting

If you encounter any issues:
1. Check the terminal output for error messages
2. Ensure all system requirements are met
3. Verify network connectivity
4. Check resource availability (RAM, CPU)

For more detailed information, visit:
{% if osVariant == 'macos' %}
- [macOS Docker Container Documentation](https://github.com/dockur/macos)
{% elif osVariant == 'windows-arm' %}
- [Windows ARM Docker Container Documentation](https://github.com/dockur/windows-arm)
{% else %}
- Windows Container Documentation
{% endif %}
